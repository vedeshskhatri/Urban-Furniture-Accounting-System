/**
 * Sound Effects — DISABLED
 * All audio effects have been removed from the site per product decision.
 * Stubs are kept so existing imports compile without changes.
 */

export function playWoodClick(_pitchMod = 1.0): void {}

export function playChimeSuccess(): void {}

export function toggleAmbientSoundscape(_onState?: boolean): boolean {
  return false;
}

export function stopAllAmbientSound(): void {}
